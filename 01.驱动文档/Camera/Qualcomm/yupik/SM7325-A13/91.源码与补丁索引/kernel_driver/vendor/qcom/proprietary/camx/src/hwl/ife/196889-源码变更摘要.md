# camx nvp6188 四路热插拔修复 — 源码变更摘要

> **说明**：本文件为 patch #196889（[HXB_[项目代号]] nvp6188 four-stream hotplug）修改源码的**变更摘要**。
> 涉及项目（[项目代号]）在 134 服务器无源码树，**源码文件不在本地归档**，仅从 patch 提取变更要点；
> 完整修改内容见 `../../../../patches/196889.patch`。补丁验证：⚠️ 无法 `git apply --check`（134 无源码树）。

**类型**：Bug 修复（四路热插拔 per-CH 解锁与内存拷贝优化）

| 文件 | 变更 | 要点 |
|:---|:---|:---|
| `camxifenode.cpp` | +32/-0 | VOID IFENode::ProcessingNodeFenceCallback( |
| `camxifenode.h` | +17/-0 | 见 patch |
| `camxnvp6188detect.cpp` | +1294/-119 | UINT        cablePresentStreak[NvpMaxCh]; // !vloss while unlocked (plug-back)；UINT        plugKickCount[NvpMaxCh];      // capped bank13 kicks (anti- |
| `camxnvp6188detect.h` | +6/-0 | VOID Nvp6188NoteRdiFence( |
| `camxchinodememcpy.cpp` | +340/-35 | 见 patch |
| `camxchinodememcpy.h` | +10/-0 | 见 patch |

## 关键代码变化

### camxifenode.cpp（+32/-0）
```
VOID IFENode::ProcessingNodeFenceCallback(
```

### camxifenode.h（+17/-0）
> 变更细节见 patch。

### camxnvp6188detect.cpp（+1294/-119）
```
UINT        cablePresentStreak[NvpMaxCh]; // !vloss while unlocked (plug-back)
UINT        plugKickCount[NvpMaxCh];      // capped bank13 kicks (anti-EPOCH)
BOOL        a8WasLost[NvpMaxCh];          // prev poll a8 lost (rising-edge primary)
UINT        primarySticky;                // last rising-edge lost CH (§77)
+static UINT64 NvpNowMs()
static BOOL NvpPatternOnUnlockEnabled()
static NvpReso NvpVfcToReso(UINT8 vfc)
VOID Nvp6188NoteRdiFence(
```

### camxnvp6188detect.h（+6/-0）
```
VOID Nvp6188NoteRdiFence(
```

### camxchinodememcpy.cpp（+340/-35）
> 变更细节见 patch。

### camxchinodememcpy.h（+10/-0）
> 变更细节见 patch。

_Author: wangguanran_
